[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "spanweave"
version = "0.1.0"
description = "Filesystem-first reproducibility tooling for AI-assisted software engineering."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
  "anthropic>=0.50.0",
  "click>=8.1.0",
  "httpx>=0.27.0",
  "openai>=1.0.0",
  "pydantic>=2",
  "python-frontmatter>=1.1.0",
  "python-ulid>=2.7.0",
  "pyyaml>=6.0.0",
]

[project.optional-dependencies]
interactive = ["questionary>=2.0,<3.0", "rich>=13.0,<14.0"]
all = ["spanweave[interactive]"]

[project.scripts]
spanweave = "spanweave.cli.main:main"

[dependency-groups]
dev = [
  "pyright>=1.1.0",
  "pytest>=8.0.0",
  "pytest-asyncio>=0.23.0",
  "pytest-cov>=6.0.0",
  "ruff>=0.11.0",
]

[tool.uv]
default-groups = ["dev"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["spanweave", "tests"]
target-version = "py311"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]

[tool.pyright]
include = ["spanweave", "tests"]
pythonVersion = "3.11"
typeCheckingMode = "strict"
venvPath = "."
venv = ".venv"
reportMissingTypeStubs = false
